import styles from '../AdminConsole.module.css';

export function SystemControlsTab() {
  return (
    <section className={styles.card}>
      <h2 className={styles.sectionTitle}>System Controls</h2>
      <p className={styles.sectionSubtitle}>Placeholder for future admin-level toggles and platform controls.</p>
    </section>
  );
}

export default SystemControlsTab;
