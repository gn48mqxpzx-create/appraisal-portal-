{
  "name": "@appraisal/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "echo \"Database package has no build output\"",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "seed:market-matrix": "node prisma/seedMarketValueMatrix.mjs",
    "backfill:standardized-roles": "node prisma/backfillStandardizedRoles.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  }
}
